# Datasync Service

## Project Setup

## API

## Docker commands

```
# Build
docker build -t datasync-service .

# Run
docker run -d -p 80:80 -t datasync-service

# Stop
docker ps
docker stop <container id>

```